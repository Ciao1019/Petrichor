# wrangler 本地预览（pnpm cf:preview / wrangler dev）使用的环境变量。
# 复制为 .dev.vars 后填入真实值；.dev.vars 已被 .gitignore 忽略。
# 生产环境请用 `wrangler secret put <NAME>` 或 Cloudflare 控制台设置，不要提交明文。
#
# ⚠️ Cloudflare Workers 只支持 PostgreSQL（不支持 better-sqlite3）。
#    DATABASE_URL 必须使用 Supabase 的连接池串（Session Pooler / Transaction Pooler），
#    即 6543 端口的 pooler 主机，而非 5432 直连。

DATABASE_URL="postgres://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres"

SESSION_SECRET="replace-with-at-least-32-characters"
PETRICHOR_SESSION_EXPIRE_SECONDS="172800"

PETRICHOR_ENCRYPT_KEY="replace-with-stable-random-secret"
PETRICHOR_ENCRYPT_SALT="replace-with-16-hex-chars"

S3_ENDPOINT="https://s3.example.com"
S3_REGION="us-east-1"
S3_ACCESS_KEY_ID="your-access-key-id"
S3_SECRET_ACCESS_KEY="your-secret-access-key"
S3_BUCKET="your-bucket"
S3_UPLOAD_EXPIRE_SECONDS="900"
S3_DOWNLOAD_EXPIRE_SECONDS="3600"
S3_USE_SSL="true"

NEXT_PUBLIC_REGISTER_ENABLED="false"
PETRICHOR_REGISTER_DEFAULT_SYSTEM_ROLE="USER"
NEXT_PUBLIC_APP_URL="https://your-worker.workers.dev"

PETRICHOR_LINUXDO_CLIENT_ID=""
PETRICHOR_LINUXDO_CLIENT_SECRET=""
PETRICHOR_LINUXDO_REDIRECT_URI=""

UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
