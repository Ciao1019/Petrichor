# 前台问答大纲部署记录

2026-09-23 已部署至 <https://wl.do>，服务器 `216.23.81.56`，版本 `qa-toc-20260923-170605`。

## 发布内容

- 相比上版 `chatfix-20260923-164821`，4 个前端文件发生变化，包含新增 `public-qa-toc.tsx`。
- 前台问答复用文章页目录：桌面悬浮大纲、手机目录抽屉；与后台共用对话条目、滚动跟踪及点击定位。
- 隐藏聊天滚动条，调整短桌面视口的目录层级；滚到底部时正确激活末尾短消息。
- 仅更新 Web，API、Worker 及其他服务容器和启动时间保持不变，无数据库变更。业务配置、Caddy 和数据卷保持原状。
- 完整发布源码 1,522 个文件均通过 SHA-256 校验；切换前再次确认工作区与快照一致。

## 验证

- Web TypeScript 检查、本次 4 个文件的 ESLint、文件行数检查和 `git diff --check` 通过。
- 助手页面、消息工具、公开问答历史与指纹相关 4 个测试文件、22 项测试通过（`--maxWorkers=2`）。
- 服务器 Web 镜像生产构建及静态资源预压缩成功，保留既有依赖外部化、dashjs 模块格式及大 chunk 提示。
- 新 Web 重启计数为 0，API healthy，API / Worker / Web 近期日志未发现 error / panic / fatal。
- 公网首页、`/ask`、`/wiki`、`/about`、`/dashboard/inbox`、`/healthz`、`/readyz` 均返回 200；首页和 32 个入口及问答相关资源摘要与新镜像一致。首次公网核验有一次 TLS 握手超时，重试完整核验后通过。
- ego-browser 使用线上真实构建与页面内临时对话夹具，未写入真实浏览器历史、未向模型发起请求：
  - 1280×600 桌面：6 个目录项均可点击；首条定位到滚动顶部，末条定位到底部并正确高亮；聊天滚动条隐藏，无横向溢出。
  - 390×844 手机：目录抽屉正常展开；点击首条立即定位并收起抽屉，输入框保持可见，无横向溢出。
  - 捕获的浏览器错误为 0；验收页面已关闭。
- 本次仅前端更新，未重复运行后端检查和全量前端 lint；上次记录中的 3 处既有未使用代码错误未纳入本次修改。

## 镜像与恢复

- Web：`petrichor-web:qa-toc-20260923-170605`。
- 镜像 ID：`sha256:a867063e6ac2c6a88ff5bf7d0a0478381fb64f3b672ffd35e3cb6f939cfd8a90`。
- API / Worker 继续使用 `petrichor-api:chatfix-20260923-164821`。
- 服务器发布源码、构建、部署及核验记录：`/opt/petrichor/releases/qa-toc-20260923-170605`。
- 旧配置及容器/镜像记录：`/opt/petrichor/backups/qa-toc-20260923-170605`，仅 root 可访问。
- 回滚命令：`python3 /opt/petrichor/releases/qa-toc-20260923-170605/deploy.py rollback`。脚本校验配置未被后续发布修改后，只恢复上版 Web；不操作数据库或数据卷。
