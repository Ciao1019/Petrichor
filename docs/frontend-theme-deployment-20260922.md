# 字体与昼夜主题部署记录

2026-09-22 已部署至 <https://wl.do>，发布标识 `theme-20260922-v1`。

## 发布内容

- 中文使用自托管 Luo；英文和数字恢复 Maple Mono，保留原有正体、斜体作用域，禁用中文合成斜体。
- 前台导航以现有昼夜切换组件替换搜索图标，沿用切换动画；浅色模式使用 Kami 纸面配色。
- 修复作者卡片浅色悬停时背景与签名同色的问题，同时调整加载占位和头像占位。
- 基于线上 `sidebar-20260922-v2` 的源码叠加 41 个文件；两个路由文件仅应用主题补丁，保留已上线的采集、侧栏及其他业务功能。
- 移除 76 个废弃字体及授权文件和旧字体 CSS，保留 Maple Mono 两个字库及 Luo 授权文件。
- 只更新 Web。API、Worker 的容器 ID 与启动时间均未改变，没有数据库迁移。

## 验证

- 本地生产构建、作者卡片与导航 7 项测试、定向 ESLint 通过。
- 服务器在实际发布源码上完成 TypeScript 检查、生产构建与静态资源压缩。
- 公网 `/`、`/about`、`/wiki`、`/healthz`、`/readyz` 均返回 200。
- 32 个入口资源及字体文件的 SHA-256 与新镜像一致，三个字体文件的 WOFF2 文件头验证通过。
- 首页 SHA-256：`011b8ec68305a152d854c1d58b0b69e003ebdf0621600c89aadc64beda4fb602`。
- Web、API、Worker 运行正常、重启计数为 0；检查时最近日志未发现 error、panic 或 fatal。
- 线上浏览器确认昼夜切换、跨页主题记忆及浅色作者卡片悬停正常；实际字形为 Luo 正体、Maple Mono 正体和斜体，390px 手机视口无横向溢出，验收页面无失败资源请求。
- 构建保留既有 dashjs 模块格式及大 chunk 提示。

## 发布与回滚

- Web 镜像：`petrichor-web:theme-20260922-v1`。
- 镜像 ID：`sha256:513341c8fce12ffb344428546671c6d7446274f420c64946f246b23d85e284b7`。
- 服务器发布源码、文件清单、构建和上线日志：`/opt/petrichor/releases/theme-20260922-v1`。
- 上版 Compose 配置和容器记录：`/opt/petrichor/backups/theme-20260922-v1`。
- 回滚脚本：`/opt/petrichor/releases/theme-20260922-v1/rollback.sh`，恢复 `sidebar-20260922-v2` 的 Web 镜像与 Compose override。
- 发布流程在切换前检查配置及原容器未发生变化；服务启动或公网静态资源校验失败会自动回滚 Web。
- 保留服务器原有业务配置、Caddy、数据库、Redis、工具服务和数据卷。登录凭据未写入仓库或发布文件。
