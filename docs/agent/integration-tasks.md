# Agent 能力集成任务

范围：在现有 Pi Agent Core、Go 权限与业务工具基础上增加能力；不接入 Langfuse 或其他评测体系。各项实现后执行定向测试，再做整体检查。

- [x] 1. 外部 MCP Client：配置服务、发现工具、白名单调用、写操作确认、超时与隔离测试。
- [x] 2. 文件化 Skills：加载 SKILL.md、校验工具与依赖、接入动态技能目录、提供助手和 Wiki 文档 Agent 示例。
- [x] 3. 模型 Reranker：接入知识库召回、失败回退本地排序、保留诊断。
- [x] 4. 沙箱与浏览器：受限代码执行、通过 MCP 接入 Playwright 交互浏览器、配置与部署说明。
- [x] 5. Pi 交互与恢复：运行中追加指令、取消、持久检查点恢复，接入助手界面；恢复保留技能说明、证据、观察和累计预算。
- [x] 6. 验证与交付：Go、Pi、前端相关检查，说明需要用户配置的外部服务及尚未覆盖的端到端验证。

实现阶段约定：新外部连接由管理员在 config.toml 明确配置；保留现有未提交修改。完成代码后，已根据用户后续明确授权部署到服务器，见 [部署记录](deployment-20260922.md)。本机真实密钥配置未修改，代码未提交或推送。

## 验证记录（2026-09-22）

- Go 全量 `go test ./...`、`go vet ./...`、`go build ./...` 通过；相关包竞态检查通过。Pi TypeScript 检查和构建通过；仓库文件行数、请求 Context 与 diff 格式检查通过。
- 官方 MCP SDK 的 HTTP 测试服务验证了工具发现、白名单、认证、参数校验、连接复用及用户/会话隔离。
- Reranker 使用模拟 HTTP 服务验证协议、结果校验和失败回退，未调用真实付费服务。
- 真实 Docker 沙箱验证了 Python/JavaScript 执行、无网络、只读文件系统和超时清理。
- 独立临时 PostgreSQL 验证了检查点、租约、并发与恢复权限；后续授权部署已备份生产数据库并应用检查点迁移。
- Pi 测试覆盖 steer、followUp、轮次上限下的排队指令与回答替换；运行时恢复测试覆盖已保存观察和 Skill 指令。
- 前端相关 10 个测试文件共 91 项测试通过；后续上下文模块整理后，再次通过运行控制组件测试、TypeScript 检查和相关文件 ESLint。前端生产构建通过；全仓库 ESLint 仍有已有的无关问题。
- 浏览器组件预览完成补充指令、恢复按钮、暗色状态及 390px 窄屏 DOM/布局检查；截图工具超时，未完成截图视觉验收，也未连接完整应用后端进行端到端验收。
- 部署时补充了真实 Playwright MCP 工具发现、Chromium 导航、页面读取和两会话浏览器隔离测试，以及生产 API 容器到 MCP/沙箱的连接与执行测试。主站首页、健康检查、就绪检查和入口静态资源返回正常。

## 启用条件

代码实现已接线，模板中的外部集成默认关闭。此次服务器部署已启用文件 Skills、浏览器 MCP、沙箱和检查点；外部 Reranker 缺少服务凭据，继续使用本地排序。其他环境按 [扩展配置与运行控制](extensions.md) 配置并部署。

检查点恢复目前用于助手，由用户显式继续；Wiki 构建保留现有 Asynq 重试流程。恢复不包含浏览器进程或沙箱文件。文件化 Skill 加载说明和已注册工具，不执行任意插件脚本。
